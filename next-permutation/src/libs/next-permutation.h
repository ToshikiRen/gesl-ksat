#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

void numberToVector(int , int**, int*);
int next_permutation(int number);
void reverseVector(int*, int, int);
int toNumber(int*, int);